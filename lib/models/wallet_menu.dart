class WalletMenuModel{

  String svgUrl;
  String price;
  String description;
  String date;
  bool dividerVisibility;

  WalletMenuModel(this.svgUrl, this.price, this.description, this.date,
      this.dividerVisibility);
}